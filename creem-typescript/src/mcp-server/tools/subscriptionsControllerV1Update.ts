/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { subscriptionsControllerV1Update } from "../../funcs/subscriptionsControllerV1Update.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.SubscriptionsControllerV1UpdateRequest$inboundSchema,
};

export const tool$subscriptionsControllerV1Update: ToolDefinition<typeof args> =
  {
    name: "subscriptions-controller-v1-update",
    description: `Update a subscription.`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await subscriptionsControllerV1Update(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
