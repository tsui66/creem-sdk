/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { productsControllerV1Search } from "../../funcs/productsControllerV1Search.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ProductsControllerV1SearchRequest$inboundSchema,
};

export const tool$productsControllerV1Search: ToolDefinition<typeof args> = {
  name: "products-controller-v1-search",
  description: `List all products`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await productsControllerV1Search(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
