/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { listTransactions } from "../../funcs/listTransactions.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListTransactionsRequest$inboundSchema,
};

export const tool$listTransactions: ToolDefinition<typeof args> = {
  name: "list-transactions",
  description: `List all transactions`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await listTransactions(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
