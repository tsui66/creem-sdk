/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { subscriptionsControllerV1Upgrade } from "../../funcs/subscriptionsControllerV1Upgrade.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.SubscriptionsControllerV1UpgradeRequest$inboundSchema,
};

export const tool$subscriptionsControllerV1Upgrade: ToolDefinition<
  typeof args
> = {
  name: "subscriptions-controller-v1-upgrade",
  description: `Upgrade a subscription to a different product`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await subscriptionsControllerV1Upgrade(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
